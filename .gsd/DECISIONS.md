# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Generalization/public distribution scope for Modular Fortress | Private project — no generalization, no install script, no fresh-droplet test. Built for Nathan's infrastructure only. | Project is no longer public. Single-user constraint in CLAUDE.md already stated this; Modular Fortress.md spec was from the older public vision. Requirements R003, R006 moved to out-of-scope; R008 deferred. | Yes | human |
| D002 |  | architecture | Old pre-Nine-Tables schema files vs new schema | Deleted 16 old root-level SQL files; promoted nine-tables/ contents to noosphere-schema/schema/ as canonical schema source of truth | New schema supersedes old (adds identity + temporal tables, IF NOT EXISTS guards, updated triggers). Old files had zero references anywhere in repo. No reason to keep dead weight. | Yes | human |
| D003 |  | conventions | GSD terminology mapping to project's existing hierarchy | Use Phase/Goal/Task in all communication with Nathan. GSD internal files retain M001/S01/T01 identifiers but human-facing language maps: Milestone→Phase, Slice→Goal, Task→Task. Matches the_work table hierarchy: Projects→Phases→Goals→Tasks. | Nathan's database already has this hierarchy in the_work table. Using consistent vocabulary avoids confusion between GSD tracking and the project's own data model. | Yes | human |
| D004 |  | conventions | Project hierarchy terminology alignment | Projects → Milestones → Goals → Tasks. DB will adopt 'milestone' instead of 'phase' to align with GitHub Projects. GSD 'slice' = 'goal' in conversation. GSD files retain M001/S01/T01 identifiers internally. | Aligns DB, GSD, and GitHub Projects on shared vocabulary. Only one translation remains: slice→goal. Previous decision D003 (phase/goal/task) is superseded by this. | Yes | collaborative |
| D005 |  | architecture | Three-pillar naming and scope for Modular Fortress | Dragonpunk (Go) replaces ALL Rust code: dpn-core, dpn-api, noosphere, and any other dpn-* crates. Noosphere Ghosts (Lisp) and InnateScipt (Lisp) stay as-is. These three pillars plus noosphere-schema (SQL) ARE Modular Fortress. Target: Mac Studio local first, then selective services to droplet later. | Nathan clarified that noosphere/ was an intermediate Rust consolidation step, not a separate pillar. Dragonpunk is the single Go binary replacing everything Rust. Simplifies the removal milestone — it's all Rust directories at once. | Yes | human |
| D006 |  | architecture | Go pillar directory name | dragonpunk/ — not membrane/, not go/, not cmd/ | Dragonpunk is the pillar name per project spec. It replaces all Rust code (dpn-core, dpn-api, noosphere). Directory name should match the pillar identity, not be generic. | Yes | collaborative |
| D007 |  | library | Go framework and driver choices for Dragonpunk | stdlib net/http (Go 1.22+ method routing), pgx v5 for PostgreSQL, godotenv for .env, log/slog for logging | Go 1.22+ stdlib has method-based routing (GET /path) making chi/gin unnecessary at this stage. pgx is the most performant native Go PostgreSQL driver. godotenv matches existing dotenvy convention from Rust. slog is stdlib structured logging — no external dependency needed. | Yes | agent |
| D008 |  | architecture | Legacy table disposition after Nine Tables migration | All 83 legacy tables dropped. Only the 14 Nine Tables remain in master_chronicle (742,673 rows). nebulab_user granted full access to all Nine Tables. | Migration was already completed predawn (commit 838ef9b, 590K+ rows). The legacy tables were stale remnants from the dump restore. Nine Tables are the sole schema going forward — Dragonpunk, Ghosts, and InnateScipt all build on this. | Yes | human |
| D009 |  | architecture | Application delivery model for Modular Fortress UI | Wails desktop app (Go backend + webview), not a web app or browser-served SPA | Modular Fortress runs on the Mac Studio and HyprDeck as a native desktop application. Go membrane already exists. Wails gives native window + webview + direct Go↔JS binding without HTTP overhead. Single binary distribution. Cross-compiles to macOS and Linux. Tauri rejected because it's Rust-based and the project is moving away from Rust. | Yes | collaborative |
| D010 |  | ui | Primary UI model for the desktop application | Foundry VTT-style interface: scene canvas + collapsible icon-rail sidebars + floating windows | Foundry VTT is the direct inspiration. Full-viewport scene canvas (maps, themed backgrounds) with ghost tokens positioned on it. Two collapsible sidebars as icon rails. Floating windows for editing records (journal entries, tasks, notes). Drag-and-drop as primary interaction — drag notes onto scenes, drop notes onto ghost tokens for assignment. This replaces the BumpTop wooden-desk metaphor as the primary UI model while potentially retaining aesthetic elements. | Yes | collaborative |
| D011 |  | ui | Sidebar layout and behavior | Both LHSB and RHSB are collapsible icon rails. LHSB for scene tools (select, place token, pin note). RHSB for content (ghost roster, domain views, chat, graph, pipelines). Gear icon at bottom of LHSB opens vault-map for domain configuration. | Follows Foundry VTT convention (RHSB = content sidebar, LHSB = scene tools) but improves on it by making both sides collapsible to icon rails like VS Code's activity bar. Icons always visible, clicking opens slide-out panel or floating window. Canvas stays dominant. | Yes | collaborative |
| D012 |  | ui | Default scene for the application | Orbis map as default scene. Initially Azgaar JPEG with positioned markers, eventually custom-rendered from the_realms coordinate data in the Modular Fortress aesthetic. | The Orbis map is already loaded in Foundry VTT with full geographic data. the_realms schema has latitude/longitude, cell references, state/province/burg hierarchy, population, biomes — everything needed for a future custom renderer. Azgaar JPEG as scene background gets 80% of the value immediately. Long-term goal: refactor AFWG rendering to produce a cleaner map from exported data. | Yes | collaborative |
| D013 |  | ui | Pipeline Builder vs Project Management — two distinct UI surfaces | Pipeline Builder is an n8n-style drag-and-drop node canvas for designing recurring operational workflows (blog publishing, trade signals, etc.). Project Management is a separate surface (milestone/task hierarchy) for one-off projects. They are not the same tool. | Pipelines repeat — every blog post flows through the same chain. Projects like Modular Fortress are one-off with unique decomposition each time. Pipeline Builder lives in Forge Workshop. Project view is a separate panel/scene. Conflating them would produce a tool that does neither well. | Yes | collaborative |
| D014 |  | architecture | Project management data flows through master_chronicle, not flat files | GSD project/milestone/task data writes to the_work in master_chronicle PostgreSQL as source of truth. GitHub Projects serves as a bidirectional mirror and additional input surface (issues flow in, status updates flow out). GSD flat files (.gsd/) become a local cache/render of database state, not the primary store. | Core value: the database is the source of truth. Currently GSD writes markdown files that nothing else can query. Writing to the_work means: (1) ghosts can see project state through normal perception queries, (2) the Modular Fortress UI can render project views from the same data, (3) GitHub becomes an external mirror rather than a parallel system, (4) Executive ghost and GSD agent operate on the same substrate. the_work schema already has task/goal/decision/issue kinds. | Yes | collaborative |
| D015 |  | ui | TypeScript as UI layer language | TypeScript rendered inside the Wails webview for all frontend components | Already specified in project constraints. Provides type safety for complex UI (scene canvas, floating windows, drag-and-drop, pipeline node editor). Framework choice (React/Svelte/vanilla) deferred — existing mockups are vanilla HTML/CSS/JS and work well, but structure will be needed as complexity grows. | Yes | collaborative |
| D016 |  | architecture | Protocol adapter architecture for the_post | Hotswappable plugin interface for protocol adapters. Go interface contract (Connect/Disconnect/Listen/Send). Adapters can be compiled-in packages or standalone binaries in a plugin directory. Config stored in database. v2.0 ships with RSS and internal adapters only. IRC/XMPP/Matrix/email added as plugins later. | the_post is the bidirectional universal messaging bus — all protocols flow through it. Building the adapter interface now means federation protocols slot in cleanly without modifying core code. Standalone binary option means Engineering ghosts can build new adapters as part of their pipeline work. Matches the "proprietary dependencies contradict sovereignty — build custom, don't integrate" philosophy. | Yes | collaborative |
| D017 |  | architecture | First protocol adapter plugin target | IRC adapter as the first external protocol plugin, targeting v2.1. Serves as the reference implementation for the hotswappable adapter interface. | IRC is the simplest real protocol — well-documented, text-only, stateless messages, no encryption complexity, libraries available in every language. Building it as the first plugin proves the adapter interface works end-to-end (bidirectional message flow through the_post, config from database, lifecycle management). Also aligns with the federation roadmap (IRC before Matrix/XMPP). If the IRC adapter works cleanly, every subsequent protocol follows the same pattern. | Yes | collaborative |
| D018 |  | ui | Sidebar content assignment — LHSB vs RHSB | LHSB = sovereign domain browsers (Chronicles, Realms, Press, Markets, Music, Forge, Commons, Work, Post). Each domain icon opens a folder-hierarchy slider for browsing/editing entries. RHSB = composed views that draw from or span domains (Reader, Journal, Chat, Ghost Roster, Graph, Scenes). Gear icon = settings/options where domain icon behaviors can be configured. Supersedes D011 LHSB=scene-tools assignment. | Domains are the structural pillars — they deserve their own sidebar as primary navigation into the data. The RHSB holds views that are composed FROM domain data (Reader reads the_commons, Journal reads temporal, Chat reads the_post, Ghost Roster reads the_forge). This mirrors the Nine Tables architecture: left side = tables, right side = views built on top of tables. Scene tools (select, place, measure) become toolbar controls on the canvas itself rather than a sidebar. | Yes | collaborative |
| D019 |  | ui | Unified entity interaction pattern across all Nine Tables | Every entity (ghost, domain entry, tool) has the same interaction model: (1) icon/avatar from the icon: field or avatar image, (2) drag onto scene = positioned token, (3) double-click = floating viewer/editor, (4) activity bubbles when entity does something, (5) drag into arrangements with other entities to compose pipelines. The scene canvas IS the workspace for composing workflows — no separate pipeline builder UI needed. | One consistent pattern is learnable and composable. Ghosts, tasks, articles, burg entries — they all behave the same way. This means the pipeline builder is emergent from scene interaction rather than a separate n8n-style tool. Drop ghosts and objects on a scene, draw connections, that IS the pipeline. Supersedes the n8n-specific canvas aspect of D013 — the distinction between pipelines (recurring) and projects (one-off) remains, but both are composed on the scene canvas using the same drag-and-drop pattern. | Yes | collaborative |
| D020 |  | ui | Journal panel structure | Journal panel (RHSB) has calendar grid at top-left for date navigation. Body shows the daily note for selected date rendered as markdown. Toggle to edit mode for writing Morning Pages, logging activity, etc. Daily note template lives in the_commons as kind='template:daily'. Ghosts update their designated sections of the daily note via Innate instructions against the template structure. | Replaces the Obsidian daily note workflow. Template-driven means consistent structure across days. Ghost-writable sections mean the daily note becomes a collaborative document — you write Morning Pages in your section, ghosts fill in activity reports, task completions, and overnight synthesis in theirs. All backed by temporal table in master_chronicle. | Yes | collaborative |
| D021 |  | ui | Ghost roster panel organization | Ghost roster (RHSB) organized as collapsible team folders (Engineering, Creative, Executive, Operations, Research, etc.). Each ghost shows avatar image + name. Avatar is the drag handle — grab and drop on scene to place ghost token. Speech bubbles appear over scene tokens when ghost posts to the_post. Same interaction pattern as all entities. | Team folders match the department structure already in the ghost data. Collapsible keeps the roster manageable with 64 ghosts. Avatar-as-drag-handle is consistent with the unified entity pattern — the visual representation IS the thing you interact with. | Yes | collaborative |
| D022 |  | ui | Wikilink graph as a navigable scene | The wikilink graph is its own scene type (not just a panel). Force-directed graph visualization rendered on the scene canvas. Nodes are color-coded by source domain (each sovereign domain has its own color from the vault-map palette). Clicking a node opens the entry's floating viewer/editor. Edges show wikilink connections from the_links table. Can be used as a navigational tool — follow connections, explore relationships, open and edit entries from within the graph. Same entity interaction pattern: double-click to open, drag to reposition. | Obsidian's graph view is one of its most powerful navigation tools. Making it a scene rather than a panel means it gets the full canvas — the graph can be large and complex with 742K+ rows of data producing potentially thousands of edges. As a scene, you can pan, zoom, filter by domain, and use it as a primary navigation surface. The domain color-coding leverages the existing vault-map color system (epic purple for Chronicles, realm green for Realms, police orange for Press, etc.). the_links table already has source_table which maps directly to domain colors. | Yes | collaborative |
| D023 |  | architecture | Real-time UI updates via PostgreSQL LISTEN/NOTIFY + Wails event binding | PostgreSQL triggers emit NOTIFY on row changes to domain tables. Go membrane listens with pgx and pushes events to the TypeScript UI via Wails native event binding. No WebSocket server needed — Wails bridge handles Go→UI communication directly. UI subscribes to change events by table/kind and updates scene canvas, chat, ghost tokens, badges in real-time. | LISTEN/NOTIFY is zero-cost at rest, millisecond delivery, built into PostgreSQL — no additional infrastructure. Wails event binding replaces WebSocket, which was the old plan (D014 roadmap Phase 6). The notification is small (table, op, id, kind, slug — well under 8KB pg_notify limit); UI fetches full row only when needed. This is simpler than the Rust LISTEN/NOTIFY → WebSocket → browser chain the old architecture described. | Yes | collaborative |
| D024 |  | architecture | Two-switch architecture — headless runtime + optional UI | The system operates as two independent processes. Switch 1: the ghost runtime (Lisp tick engine + Go membrane + PostgreSQL) runs headless as a background service — ghosts tick, adapters poll, the noosphere lives. Switch 2: the Wails desktop UI opens a window onto the running system — it connects to the same database and receives LISTEN/NOTIFY events. The UI is a viewport into an already-running world, not the world itself. Closing the UI doesn't stop the ghosts. | This is the artificial life model. The noosphere is alive whether you're watching or not. Ghosts tick on their circadian rhythms (03:00 leisure, 04:00 synthesis, 06:00 dawn). RSS feeds get fetched. Pipeline stages advance. Memory consolidation happens. Opening the UI is like walking into your office and looking at the monitors — the work was already happening. This also means the system works on headless Linux servers (droplet) where there's no display, and works on the Mac Studio where you want to see what's going on. | Yes | collaborative |
