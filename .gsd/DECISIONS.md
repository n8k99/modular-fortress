# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Generalization/public distribution scope for Modular Fortress | Private project — no generalization, no install script, no fresh-droplet test. Built for Nathan's infrastructure only. | Project is no longer public. Single-user constraint in CLAUDE.md already stated this; Modular Fortress.md spec was from the older public vision. Requirements R003, R006 moved to out-of-scope; R008 deferred. | Yes | human |
| D002 |  | architecture | Old pre-Nine-Tables schema files vs new schema | Deleted 16 old root-level SQL files; promoted nine-tables/ contents to noosphere-schema/schema/ as canonical schema source of truth | New schema supersedes old (adds identity + temporal tables, IF NOT EXISTS guards, updated triggers). Old files had zero references anywhere in repo. No reason to keep dead weight. | Yes | human |
| D003 |  | conventions | GSD terminology mapping to project's existing hierarchy | Use Phase/Goal/Task in all communication with Nathan. GSD internal files retain M001/S01/T01 identifiers but human-facing language maps: Milestone→Phase, Slice→Goal, Task→Task. Matches the_work table hierarchy: Projects→Phases→Goals→Tasks. | Nathan's database already has this hierarchy in the_work table. Using consistent vocabulary avoids confusion between GSD tracking and the project's own data model. | Yes | human |
| D004 |  | conventions | Project hierarchy terminology alignment | Projects → Milestones → Goals → Tasks. DB will adopt 'milestone' instead of 'phase' to align with GitHub Projects. GSD 'slice' = 'goal' in conversation. GSD files retain M001/S01/T01 identifiers internally. | Aligns DB, GSD, and GitHub Projects on shared vocabulary. Only one translation remains: slice→goal. Previous decision D003 (phase/goal/task) is superseded by this. | Yes | collaborative |
| D005 |  | architecture | Three-pillar naming and scope for Modular Fortress | Dragonpunk (Go) replaces ALL Rust code: dpn-core, dpn-api, noosphere, and any other dpn-* crates. Noosphere Ghosts (Lisp) and InnateScipt (Lisp) stay as-is. These three pillars plus noosphere-schema (SQL) ARE Modular Fortress. Target: Mac Studio local first, then selective services to droplet later. | Nathan clarified that noosphere/ was an intermediate Rust consolidation step, not a separate pillar. Dragonpunk is the single Go binary replacing everything Rust. Simplifies the removal milestone — it's all Rust directories at once. | Yes | human |
| D006 |  | architecture | Go pillar directory name | dragonpunk/ — not membrane/, not go/, not cmd/ | Dragonpunk is the pillar name per project spec. It replaces all Rust code (dpn-core, dpn-api, noosphere). Directory name should match the pillar identity, not be generic. | Yes | collaborative |
| D007 |  | library | Go framework and driver choices for Dragonpunk | stdlib net/http (Go 1.22+ method routing), pgx v5 for PostgreSQL, godotenv for .env, log/slog for logging | Go 1.22+ stdlib has method-based routing (GET /path) making chi/gin unnecessary at this stage. pgx is the most performant native Go PostgreSQL driver. godotenv matches existing dotenvy convention from Rust. slog is stdlib structured logging — no external dependency needed. | Yes | agent |
| D008 |  | architecture | Legacy table disposition after Nine Tables migration | All 83 legacy tables dropped. Only the 14 Nine Tables remain in master_chronicle (742,673 rows). nebulab_user granted full access to all Nine Tables. | Migration was already completed predawn (commit 838ef9b, 590K+ rows). The legacy tables were stale remnants from the dump restore. Nine Tables are the sole schema going forward — Dragonpunk, Ghosts, and InnateScipt all build on this. | Yes | human |
