---
phase: 07-structured-artifact-passing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /root/.planning/phases/07-structured-artifact-passing/migrations/001_stage_notes_jsonb_migration.sql
  - /opt/dpn-api/src/handlers/af64_tasks.rs
  - /opt/dpn-api/src/handlers/af64_perception.rs
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "stage_notes column in tasks table is JSONB type, not TEXT"
    - "Existing freeform text data is wrapped in {legacy_text, schema_version: 0} JSON"
    - "Rust API returns stage_notes as JSON objects (not strings) and accepts JSON objects for updates"
  artifacts:
    - path: "/root/.planning/phases/07-structured-artifact-passing/migrations/001_stage_notes_jsonb_migration.sql"
      provides: "SQL migration converting stage_notes TEXT to JSONB with data wrapping"
      contains: "ALTER TABLE tasks"
    - path: "/opt/dpn-api/src/handlers/af64_tasks.rs"
      provides: "Task CRUD handlers with JSONB stage_notes"
      contains: "serde_json::Value"
    - path: "/opt/dpn-api/src/handlers/af64_perception.rs"
      provides: "Perception handler with JSONB stage_notes"
      contains: "serde_json::Value"
  key_links:
    - from: "af64_tasks.rs"
      to: "tasks.stage_notes (JSONB)"
      via: "sqlx row.get and query bind"
      pattern: "serde_json::Value.*stage_notes"
    - from: "af64_perception.rs"
      to: "tasks.stage_notes (JSONB)"
      via: "sqlx row.get"
      pattern: "serde_json::Value.*stage_notes"
---

<objective>
Migrate stage_notes from TEXT to JSONB and update Rust API types to match.

Purpose: Foundation for structured artifact passing -- JSONB column enables schema validation and structured queries. Per D-04, all structured output lives in the DB. Per D-05, existing data is preserved via wrapping.
Output: Migration SQL file executed, Rust handlers updated to handle serde_json::Value instead of String for stage_notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-structured-artifact-passing/07-CONTEXT.md
@.planning/phases/07-structured-artifact-passing/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From /opt/dpn-api/src/handlers/af64_tasks.rs (CURRENT -- to be changed):
```rust
// Line 61 -- reading stage_notes as String (CHANGE TO serde_json::Value)
"stage_notes": r.get::<Option<String>, _>("stage_notes"),

// Line 76-90 -- TaskUpdate struct
pub struct TaskUpdate {
    pub stage_notes: Option<String>,  // CHANGE TO Option<serde_json::Value>
    // ... other fields unchanged
}

// Line 122-125 -- update handler binds stage_notes as String (auto-works with Value)
```

From /opt/dpn-api/src/handlers/af64_perception.rs (CURRENT -- to be changed):
```rust
// Line 195 -- reading stage_notes as String (CHANGE TO serde_json::Value)
"stage_notes": r.get::<Option<String>, _>("stage_notes"),
```

Phase 6 migration pattern (reference):
```sql
-- From 06 blocked_by migration: ALTER with USING for type conversion
ALTER TABLE tasks ALTER COLUMN blocked_by TYPE INTEGER[]
  USING CASE WHEN blocked_by IS NOT NULL ... END;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and execute JSONB migration for stage_notes</name>
  <files>
    /root/.planning/phases/07-structured-artifact-passing/migrations/001_stage_notes_jsonb_migration.sql
  </files>
  <read_first>
    /root/.planning/phases/06-task-dependency-chains/migrations/
    /root/.planning/phases/07-structured-artifact-passing/07-RESEARCH.md
  </read_first>
  <action>
    Create migrations directory and SQL file per D-04 and D-05.

    The migration SQL must:
    1. ALTER TABLE tasks ALTER COLUMN stage_notes TYPE JSONB USING CASE:
       - When stage_notes IS NOT NULL AND stage_notes != '' -> jsonb_build_object('legacy_text', stage_notes, 'schema_version', 0)
       - ELSE NULL
    2. Add a COMMENT ON COLUMN noting the schema: "JSONB artifact: schema_version 0=legacy wrapped text, 1=structured artifact"

    After creating the file, execute it:
    ```
    psql -U chronicle -d master_chronicle -f /root/.planning/phases/07-structured-artifact-passing/migrations/001_stage_notes_jsonb_migration.sql
    ```

    Verify the column type changed:
    ```
    psql -U chronicle -d master_chronicle -c "\d tasks" | grep stage_notes
    ```

    Verify existing data was wrapped (per D-05):
    ```
    psql -U chronicle -d master_chronicle -c "SELECT id, stage_notes FROM tasks WHERE stage_notes IS NOT NULL LIMIT 3;"
    ```
    Each result should show a JSON object with "legacy_text" and "schema_version": 0.
  </action>
  <verify>
    <automated>psql -U chronicle -d master_chronicle -c "SELECT data_type FROM information_schema.columns WHERE table_name='tasks' AND column_name='stage_notes';" | grep -q "jsonb" && psql -U chronicle -d master_chronicle -c "SELECT stage_notes->>'schema_version' FROM tasks WHERE stage_notes IS NOT NULL LIMIT 1;" | grep -q "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `\d tasks` shows stage_notes as jsonb (not text)
    - `SELECT stage_notes FROM tasks WHERE stage_notes IS NOT NULL LIMIT 1` returns JSON with "legacy_text" key and "schema_version": 0
    - No data loss: `SELECT count(*) FROM tasks WHERE stage_notes IS NOT NULL` returns same count as before migration (374 per research)
  </acceptance_criteria>
  <done>stage_notes column is JSONB, all 374 existing text values wrapped in {legacy_text, schema_version: 0}</done>
</task>

<task type="auto">
  <name>Task 2: Update Rust handlers for JSONB stage_notes</name>
  <files>
    /opt/dpn-api/src/handlers/af64_tasks.rs
    /opt/dpn-api/src/handlers/af64_perception.rs
  </files>
  <read_first>
    /opt/dpn-api/src/handlers/af64_tasks.rs
    /opt/dpn-api/src/handlers/af64_perception.rs
  </read_first>
  <action>
    Update both Rust files so stage_notes uses serde_json::Value instead of String. This prevents the sqlx type mismatch error described in Pitfall 2 of the research.

    In af64_tasks.rs:
    1. Line 61: Change `r.get::<Option<String>, _>("stage_notes")` to `r.get::<Option<serde_json::Value>, _>("stage_notes")`
    2. Line 86 in TaskUpdate struct: Change `pub stage_notes: Option<String>` to `pub stage_notes: Option<serde_json::Value>`
    3. Lines 122-125: The update handler bind already works with serde_json::Value via sqlx -- no query change needed, just the type change propagates.
    4. Note: NewTask struct (line 157-169) does NOT have stage_notes field -- no change needed there.

    In af64_perception.rs:
    1. Line 195: Change `r.get::<Option<String>, _>("stage_notes")` to `r.get::<Option<serde_json::Value>, _>("stage_notes")`

    After changes, build and verify:
    ```
    cd /opt/dpn-api && cargo build 2>&1
    ```

    Then restart the API:
    ```
    pm2 restart dpn-api
    ```

    Then verify the API still works:
    ```
    curl -s http://localhost:8080/api/af64/tasks?limit=3 | jq '.[0].stage_notes'
    ```
    Should return a JSON object (not a string), or null for tasks without stage_notes.
  </action>
  <verify>
    <automated>cd /opt/dpn-api && cargo build 2>&1 | tail -1 | grep -q "Finished" && curl -s http://localhost:8080/api/af64/tasks?limit=1 | jq -e '.[0]' > /dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds with no errors in /opt/dpn-api
    - `curl http://localhost:8080/api/af64/tasks?limit=3 | jq '.[0].stage_notes'` returns JSON object (with legacy_text key) or null, NOT a string
    - `curl http://localhost:8080/api/perception/eliana | jq '.tasks[0].stage_notes'` returns JSON or null, NOT a string
    - grep -n "serde_json::Value.*stage_notes\|stage_notes.*serde_json::Value" in both files shows the type changes
  </acceptance_criteria>
  <done>Rust API compiles, serves JSONB stage_notes as JSON objects, accepts JSON objects for stage_notes updates</done>
</task>

</tasks>

<verification>
1. Database: `psql -c "SELECT data_type FROM information_schema.columns WHERE table_name='tasks' AND column_name='stage_notes';"` returns "jsonb"
2. Data preserved: `psql -c "SELECT count(*) FROM tasks WHERE stage_notes IS NOT NULL;"` returns ~374
3. Legacy wrapping: `psql -c "SELECT stage_notes->>'legacy_text' IS NOT NULL FROM tasks WHERE stage_notes IS NOT NULL LIMIT 1;"` returns true
4. API builds: `cd /opt/dpn-api && cargo build` succeeds
5. API serves JSONB: `curl -s http://localhost:8080/api/af64/tasks?limit=1 | jq '.[0].stage_notes | type'` returns "object" or "null"
</verification>

<success_criteria>
- stage_notes is JSONB in PostgreSQL
- Existing 374 text values wrapped as {legacy_text, schema_version: 0}
- Rust API compiles and serves stage_notes as JSON
- No 500 errors on task or perception endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/07-structured-artifact-passing/07-01-SUMMARY.md`
</output>
