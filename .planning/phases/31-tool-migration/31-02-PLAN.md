---
phase: 31-tool-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /opt/project-noosphere-ghosts/config/agents/nova.yaml
  - /opt/project-noosphere-ghosts/config/agents/eliana.yaml
  - /opt/project-noosphere-ghosts/config/agents/kathryn.yaml
  - /opt/project-noosphere-ghosts/config/agents/sylvia.yaml
  - /opt/project-noosphere-ghosts/config/agents/vincent.yaml
  - /opt/project-noosphere-ghosts/config/agents/jmax.yaml
  - /opt/project-noosphere-ghosts/config/agents/lrm.yaml
  - /opt/project-noosphere-ghosts/config/agents/sarah.yaml
  - /opt/project-noosphere-ghosts/config/agents/ethan_ng.yaml
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "Every ghost YAML has complete tool capabilities matching its departmental role"
    - "All 9 ghost YAMLs include read_own_memory and write_own_memory (universal tools)"
    - "No ghost has tools outside its domain (Vincent does NOT have trading tools, etc.)"
    - "Existing non-tool responsibilities and orbis/persona sections are preserved"
  artifacts:
    - path: "/opt/project-noosphere-ghosts/config/agents/nova.yaml"
      provides: "Nova COO capabilities — ops, temporal, health checks, query_db, claude_code"
      contains: "![ops_health_check]"
    - path: "/opt/project-noosphere-ghosts/config/agents/kathryn.yaml"
      provides: "Kathryn CSO capabilities — all trading/market/risk tools"
      contains: "![trade_executor]"
    - path: "/opt/project-noosphere-ghosts/config/agents/eliana.yaml"
      provides: "Eliana CTO capabilities — engineering, build, code analysis tools"
      contains: "![codebase_scanner]"
  key_links:
    - from: "ghost YAML responsibilities"
      to: "area_content tool names"
      via: "![tool_name] matching title column"
      pattern: "!\\[.*\\]"
---

<objective>
Update all 9 ghost YAML files with complete InnateScipt tool capability declarations per D-01, D-02, D-11.

Purpose: Every ghost must declare its full tool capabilities as `![tool_name]` expressions before tool-registry.json can be retired. Capabilities map to departmental roles per CLAUDE.md executive roster.
Output: 9 updated YAML files with complete responsibilities sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-tool-migration/31-CONTEXT.md
@.planning/phases/31-tool-migration/31-RESEARCH.md

<interfaces>
<!-- Ghost YAML format established in Phase 28/29 -->
<!-- Responsibilities are a top-level list of InnateScipt expressions -->
<!-- write-ghost-yaml in ghost-capabilities.lisp preserves non-responsibility sections -->
<!-- Format: plain YAML list items under responsibilities: key -->

Example existing structure (nova.yaml):
```yaml
id: nova
ship_assignment: "ISS Pinnacle of Society"
starting_point:
  x: 0
  y: 0
rpg_persona:
  deity_codename: "Hermes"
  ...
orbis_access:
  energy_min: 20
  trust_min: 30
responsibilities:
  - "![query_db]"
  - "![pipeline_status]"
  - "![claude_code]"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 9 ghost YAML files with complete tool capabilities</name>
  <files>
    /opt/project-noosphere-ghosts/config/agents/nova.yaml
    /opt/project-noosphere-ghosts/config/agents/eliana.yaml
    /opt/project-noosphere-ghosts/config/agents/kathryn.yaml
    /opt/project-noosphere-ghosts/config/agents/sylvia.yaml
    /opt/project-noosphere-ghosts/config/agents/vincent.yaml
    /opt/project-noosphere-ghosts/config/agents/jmax.yaml
    /opt/project-noosphere-ghosts/config/agents/lrm.yaml
    /opt/project-noosphere-ghosts/config/agents/sarah.yaml
    /opt/project-noosphere-ghosts/config/agents/ethan_ng.yaml
  </files>
  <read_first>
    /opt/project-noosphere-ghosts/config/agents/nova.yaml
    /opt/project-noosphere-ghosts/config/agents/eliana.yaml
    /opt/project-noosphere-ghosts/config/agents/kathryn.yaml
    /opt/project-noosphere-ghosts/config/agents/sylvia.yaml
    /opt/project-noosphere-ghosts/config/agents/vincent.yaml
    /opt/project-noosphere-ghosts/config/agents/jmax.yaml
    /opt/project-noosphere-ghosts/config/agents/lrm.yaml
    /opt/project-noosphere-ghosts/config/agents/sarah.yaml
    /opt/project-noosphere-ghosts/config/agents/ethan_ng.yaml
    /opt/project-noosphere-ghosts/config/tool-registry.json
    .planning/phases/31-tool-migration/31-RESEARCH.md
  </read_first>
  <action>
For each ghost YAML file, ADD missing tool `![tool_name]` expressions to the responsibilities section. PRESERVE all existing entries (including non-tool expressions like `{em.strategy.portfolio}`, `{em.content.blog}`, `![pipeline_status]`, `![fundamentals.feeds]`, `![technicals.oanda_api]`). PRESERVE all non-responsibilities sections (id, ship_assignment, starting_point, rpg_persona, orbis_access).

Use the write-ghost-yaml pattern: load existing YAML, update only the responsibilities list, write back preserving other sections. Since we are editing YAML directly (not via Lisp), read each file, preserve the non-responsibilities content verbatim, and replace/extend the responsibilities list.

Per D-01, D-02, D-11, and the RESEARCH.md YAML Capability Assignment Strategy, assign tools by departmental role:

**Nova (COO) — Operations, automation, droplet:**
Keep existing: `![query_db]`, `![pipeline_status]`, `![claude_code]`
Add: `![ops_health_check]`, `![ops_daily_note]`, `![ops_nightly_synthesis]`, `![ops_weekly_rollup]`, `![ops_monthly_rollup]`, `![ops_podcast_watcher]`, `![self_improvement]`, `![read_schedule]`, `![memory_compressor]`, `![event_tracker]`, `![recurrence_engine]`, `![read_own_memory]`, `![write_own_memory]`

**Eliana (CTO) — Engineering, infrastructure, all repos:**
Keep existing: `![build_tool]`, `![claude_code]`, `![query_db]`
Add: `![list_tasks]`, `![assign_task]`, `![write_tool_spec]`, `![security_review]`, `![run_tool_tests]`, `![deploy_tool]`, `![lookup_agents]`, `![codebase_scanner]`, `![security_linter]`, `![dependency_mapper]`, `![pattern_detector]`, `![cascade_reporter]`, `![read_own_memory]`, `![write_own_memory]`

**Kathryn (CSO) — Strategy, marketing, prediction markets:**
Keep existing: `![market_scanner]`, `{em.strategy.portfolio}`
Add: `![trade_executor]`, `![thesis_writer]`, `![market_data]`, `![forex_trader]`, `![forex_sentiment]`, `![forex_fitness]`, `![kalshi_integration]`, `![kalshi_market_integration]`, `![kalshi_thesis_filter]`, `![position_tracker]`, `![risk_calculator]`, `![exposure_audit]`, `![probability_model]`, `![trade_journal]`, `![trading_briefing]`, `![market_data_ingest]`, `![market_data_processor]`, `![market_monitoring]`, `![market_dashboard]`, `![market_pulse_monitor]`, `![wave_calendar_sync]`, `![delegate_investment]`, `![news_aggregator]`, `![regulatory_check]`, `![read_own_memory]`, `![write_own_memory]`

**Sylvia (Content Chief) — All writing, narrative, content:**
Keep existing: `![write_document]`, `{em.content.blog}`, `{em.content.thought.police}`
Add: `![search_documents]`, `![article_fetcher]`, `![article_parser]`, `![article_summarizer]`, `![comment_collector]`, `![rss_feed_ingester]`, `![sentiment_analyzer]`, `![thought_police_publisher]`, `![editorial_composer]`, `![editorial_nightly]`, `![delegate_editorial]`, `![morning_pages_parser]`, `![thematic_matcher]`, `![read_own_memory]`, `![write_own_memory]`

**Vincent (Creative Director) — Visual, images, art direction:**
Keep existing: `![write_document]`, `{em.creative.covers}`
Add: `![search_documents]`, `![read_own_memory]`, `![write_own_memory]`

**JMax (Head of Legal) — Canon arbiter, legal, ethics, lore:**
Keep existing: `![query_db]`, `{em.legal.compliance}`
Add: `![regulatory_check]`, `![exposure_audit]`, `![search_documents]`, `![read_own_memory]`, `![write_own_memory]`

**LRM (Head of Musicology) — Music, audio, composition:**
Keep existing: `![query_db]`, `{em.music.archive}`
Add: `![search_documents]`, `![read_own_memory]`, `![write_own_memory]`

**Sarah Lin (Executive PA) — Routing, orchestrator of orchestrators:**
Keep existing: `![query_db]`
Add: `![list_tasks]`, `![assign_task]`, `![lookup_agents]`, `![read_schedule]`, `![read_own_memory]`, `![write_own_memory]`

**Ethan Ng (Staff — FOREX specialist):**
Keep existing: `![fundamentals.feeds]`, `![technicals.oanda_api]`, `{em.content.podcast}`
Add: `![forex_trader]`, `![forex_sentiment]`, `![forex_fitness]`, `![market_data]`, `![news_aggregator]`, `![read_own_memory]`, `![write_own_memory]`

**Worldbuilding tools** (burg_pipeline, burg_template, worldbuilding_pipeline, delegate_worldbuilding): Assign to JMax (canon arbiter) per domain routing. Add: `![worldbuilding_pipeline]`, `![delegate_worldbuilding]`, `![burg_pipeline]`, `![burg_template]` to JMax's YAML.

**Delegate tools**: delegate_modular_fortress goes to Eliana (engineering coordination). Add `![delegate_modular_fortress]` to Eliana.

**OpenClaw tools** (save_spec, create_pipeline, research_topic): If they exist, assign to Eliana. If deprecated, skip them from YAML (ghosts should not declare capabilities for tools that don't work).
  </action>
  <verify>
    <automated>for f in /opt/project-noosphere-ghosts/config/agents/*.yaml; do echo "$(basename $f): $(grep -c '!\[' $f) tools"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 9 YAML files contain `![read_own_memory]` and `![write_own_memory]`
    - nova.yaml has 15+ responsibility entries including `![ops_health_check]`, `![ops_daily_note]`
    - eliana.yaml has 16+ responsibility entries including `![codebase_scanner]`, `![build_tool]`
    - kathryn.yaml has 27+ responsibility entries including `![trade_executor]`, `![kalshi_integration]`
    - sylvia.yaml has 17+ responsibility entries including `![editorial_composer]`, `![thought_police_publisher]`
    - vincent.yaml has 4+ responsibility entries (limited domain)
    - jmax.yaml has 10+ responsibility entries including worldbuilding tools
    - sarah.yaml has 7+ responsibility entries including `![assign_task]`, `![lookup_agents]`
    - ethan_ng.yaml has 9+ responsibility entries including `![forex_trader]`, `![forex_sentiment]`
    - Existing non-tool expressions preserved (grep for `{em.` in kathryn, sylvia, vincent, jmax, lrm, ethan_ng)
    - All non-responsibilities YAML sections preserved (id, ship_assignment, starting_point, rpg_persona, orbis_access)
  </acceptance_criteria>
  <done>All 9 ghost YAMLs declare complete tool capabilities matching departmental roles per D-01 and D-11, with universal tools on every ghost and existing expressions preserved</done>
</task>

</tasks>

<verification>
- `for f in /opt/project-noosphere-ghosts/config/agents/*.yaml; do echo "$(basename $f): $(grep -c '!\[' $f) tools"; done` shows appropriate counts per ghost
- `grep -l 'read_own_memory' /opt/project-noosphere-ghosts/config/agents/*.yaml | wc -l` returns 9
- `grep '{em\.' /opt/project-noosphere-ghosts/config/agents/*.yaml` shows existing non-tool expressions preserved
</verification>

<success_criteria>
- All 9 ghost YAMLs have complete tool capability declarations
- Tool assignments match departmental roles per CLAUDE.md roster
- Universal tools (read_own_memory, write_own_memory) present on all ghosts
- Existing non-tool InnateScipt expressions preserved
- Non-responsibilities YAML sections (orbis, persona) untouched
</success_criteria>

<output>
After completion, create `.planning/phases/31-tool-migration/31-02-SUMMARY.md`
</output>
