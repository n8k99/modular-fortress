---
phase: 18-memories-rename
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - /root/dpn-core/src/db/vault_notes.rs
  - /root/dpn-core/src/db/memories.rs
  - /root/dpn-core/src/db/mod.rs
  - /root/dpn-core/src/lib.rs
  - /root/dpn-core/src/timeline/mod.rs
  - /root/dpn-core/src/sync/snapshot.rs
  - /root/dpn-core/src/sync/incremental.rs
  - /root/dpn-core/src/pipeline/tasks_due.rs
  - /root/dpn-core/src/embeddings/batch.rs
  - /root/dpn-core/src/context/injection.rs
  - /root/dpn-core/src/cache/sqlite.rs
  - /root/dpn-core/src/cache/hybrid.rs
  - /root/dpn-core/src/cache/sync_queue.rs
  - /root/dpn-core/src/db/tests.rs
  - /root/dpn-core/src/db/documents.rs
autonomous: true
requirements: [MEM-06]

must_haves:
  truths:
    - "dpn-core compiles with zero vault_notes references in production SQL strings"
    - "All public types are Memory and MemoryLight (not VaultNote)"
    - "Module is db::memories (not db::vault_notes)"
    - "cargo test passes in dpn-core"
  artifacts:
    - path: "/root/dpn-core/src/db/memories.rs"
      provides: "Memory and MemoryLight structs, all query functions"
      contains: "FROM memories"
    - path: "/root/dpn-core/src/db/mod.rs"
      provides: "pub mod memories declaration"
      contains: "pub mod memories"
    - path: "/root/dpn-core/src/lib.rs"
      provides: "Re-exports of Memory, MemoryLight"
      contains: "pub use db::memories"
  key_links:
    - from: "/root/dpn-core/src/lib.rs"
      to: "/root/dpn-core/src/db/memories.rs"
      via: "pub use db::memories::{Memory, MemoryLight}"
      pattern: "pub use db::memories"
    - from: "/root/dpn-core/src/timeline/mod.rs"
      to: "/root/dpn-core/src/db/memories.rs"
      via: "import path"
      pattern: "db::memories"
---

<objective>
Rename the dpn-core vault_notes module to memories: file rename, struct renames (VaultNote -> Memory, VaultNoteLight -> MemoryLight), all SQL strings FROM vault_notes -> FROM memories, module declarations, re-exports, and all consumer references across 15 files.

Purpose: Align Rust code with the PARAT-native table name. After this plan, dpn-core compiles and tests pass with the new naming.

Output: /root/dpn-core/src/db/memories.rs and all consumers updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-memories-rename/18-CONTEXT.md
@.planning/phases/18-memories-rename/18-RESEARCH.md

<interfaces>
<!-- Key types being renamed. Executor must find and replace ALL occurrences. -->

From /root/dpn-core/src/db/vault_notes.rs (current):
```rust
pub struct VaultNote { id, path, title, content, frontmatter, size_bytes, note_type, note_date, modified_at, created_at }
pub struct VaultNoteLight { id, path, title, size_bytes, note_type, note_date, modified_at }
// 13 query functions, all with `FROM vault_notes` in SQL strings
```

From /root/dpn-core/src/lib.rs (current):
```rust
pub use db::vault_notes::{VaultNote, VaultNoteLight};
```

From /root/dpn-core/src/db/mod.rs (current):
```rust
pub mod vault_notes;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename vault_notes.rs to memories.rs and update all internal references</name>
  <files>
    /root/dpn-core/src/db/vault_notes.rs
    /root/dpn-core/src/db/memories.rs
    /root/dpn-core/src/db/mod.rs
    /root/dpn-core/src/lib.rs
  </files>
  <read_first>
    /root/dpn-core/src/db/vault_notes.rs
    /root/dpn-core/src/db/mod.rs
    /root/dpn-core/src/lib.rs
  </read_first>
  <action>
**Per D-11, D-12: Rename dpn-core module and structs.**

1. **Rename file:** `mv /root/dpn-core/src/db/vault_notes.rs /root/dpn-core/src/db/memories.rs`

2. **Edit memories.rs** — Replace all occurrences:
   - Module doc: `//! vault_notes table access` -> `//! memories table access (renamed from vault_notes)`
   - Struct names: `VaultNote` -> `Memory`, `VaultNoteLight` -> `MemoryLight`
   - All `impl VaultNote` -> `impl Memory`, `impl VaultNoteLight` -> `impl MemoryLight`
   - All SQL strings: `FROM vault_notes` -> `FROM memories`
   - All SQL strings: `INTO vault_notes` -> `INTO memories`
   - All SQL strings: `UPDATE vault_notes` -> `UPDATE memories`
   - Doc comments: `vault note` -> `memory` (lowercase in comments)
   - tracing messages: `vault note` -> `memory`
   - Add new struct fields for compression_tier and compressed_from to Memory struct:
     ```rust
     pub compression_tier: String,
     pub compressed_from: Option<Vec<i32>>,
     ```
   - Add compression_tier to MemoryLight struct:
     ```rust
     pub compression_tier: String,
     ```
   - Update ALL SELECT queries in the file to include `compression_tier` and `compressed_from` in the column list for Memory queries, and `compression_tier` for MemoryLight queries
   - The `create` function INSERT must include compression_tier with default 'daily'

3. **Edit db/mod.rs** — Change: `pub mod vault_notes;` -> `pub mod memories;`
   Also update the module doc comment to say `memories` instead of `vault_notes`.

4. **Edit lib.rs** — Change: `pub use db::vault_notes::{VaultNote, VaultNoteLight};` -> `pub use db::memories::{Memory, MemoryLight};`

Do NOT yet update consumer files in other modules — that is handled in Task 2.
  </action>
  <verify>
    <automated>test -f /root/dpn-core/src/db/memories.rs && ! test -f /root/dpn-core/src/db/vault_notes.rs && grep -c "FROM memories" /root/dpn-core/src/db/memories.rs && grep "pub mod memories" /root/dpn-core/src/db/mod.rs && grep "pub use db::memories" /root/dpn-core/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - /root/dpn-core/src/db/memories.rs exists
    - /root/dpn-core/src/db/vault_notes.rs does NOT exist
    - `grep -c "vault_notes" /root/dpn-core/src/db/memories.rs` returns 0 (no remaining old references in SQL)
    - `grep "pub struct Memory" /root/dpn-core/src/db/memories.rs` succeeds
    - `grep "pub struct MemoryLight" /root/dpn-core/src/db/memories.rs` succeeds
    - `grep "compression_tier" /root/dpn-core/src/db/memories.rs` succeeds
    - `grep "pub mod memories" /root/dpn-core/src/db/mod.rs` succeeds
    - `grep "pub use db::memories::{Memory, MemoryLight}" /root/dpn-core/src/lib.rs` succeeds
  </acceptance_criteria>
  <done>Core module renamed with all structs, SQL, and declarations updated</done>
</task>

<task type="auto">
  <name>Task 2: Update all dpn-core consumer files (15 files) and verify compilation</name>
  <files>
    /root/dpn-core/src/timeline/mod.rs
    /root/dpn-core/src/sync/snapshot.rs
    /root/dpn-core/src/sync/incremental.rs
    /root/dpn-core/src/pipeline/tasks_due.rs
    /root/dpn-core/src/embeddings/batch.rs
    /root/dpn-core/src/context/injection.rs
    /root/dpn-core/src/cache/sqlite.rs
    /root/dpn-core/src/cache/hybrid.rs
    /root/dpn-core/src/cache/sync_queue.rs
    /root/dpn-core/src/db/tests.rs
    /root/dpn-core/src/db/documents.rs
  </files>
  <read_first>
    /root/dpn-core/src/timeline/mod.rs
    /root/dpn-core/src/sync/snapshot.rs
    /root/dpn-core/src/sync/incremental.rs
    /root/dpn-core/src/pipeline/tasks_due.rs
    /root/dpn-core/src/embeddings/batch.rs
    /root/dpn-core/src/context/injection.rs
    /root/dpn-core/src/cache/sqlite.rs
    /root/dpn-core/src/cache/hybrid.rs
    /root/dpn-core/src/cache/sync_queue.rs
    /root/dpn-core/src/db/tests.rs
    /root/dpn-core/src/db/documents.rs
  </read_first>
  <action>
**Per D-11: Update all dpn-core consumer files.**

For EACH file below, read it first, then apply ALL of these substitutions:

**Import paths:**
- `db::vault_notes` -> `db::memories`
- `crate::db::vault_notes` -> `crate::db::memories`
- `super::vault_notes` -> `super::memories`

**Type names:**
- `VaultNote` -> `Memory` (as a type name, not in strings)
- `VaultNoteLight` -> `MemoryLight`

**SQL strings (raw SQL in non-db files):**
- `FROM vault_notes` -> `FROM memories`
- `INTO vault_notes` -> `INTO memories`
- `vault_notes` in any SQL context -> `memories`

**String literals (non-SQL, e.g., enum variants, display strings):**
- `"vault_notes"` or `"VaultNotes"` as enum/type discriminators -> `"memories"` or `"Memories"`

**Variable names (cosmetic but improves readability):**
- `vault_notes` as variable name -> `memories` (where it won't break enum matching)
- `vault_note` as variable name -> `memory` (singular)

**Comments:**
- `vault_notes` in comments -> `memories` (update for accuracy)

**Files and their expected changes:**

1. **timeline/mod.rs** (~8 refs): Has `TimelineType::VaultNote` enum variant. Rename to `TimelineType::Memory`. Update match arms, display strings, SQL strings referencing vault_notes table.

2. **sync/snapshot.rs** (~6 refs): Function calls to vault_notes module, string literals. Update imports and calls.

3. **sync/incremental.rs** (~8 refs): Function calls to vault_notes module, string literals. Update imports and calls.

4. **replay/collector.rs** (~12 refs): If this file exists, update VaultNote enum references, module imports, function calls.

5. **replay/narrative.rs** (~3 refs): If this file exists, update VaultNote enum references.

6. **replay/templates.rs** (~1 ref): If this file exists, update VaultNote enum reference.

7. **pipeline/tasks_due.rs** (~8 refs): Module import + function calls + struct refs. Update all.

8. **embeddings/batch.rs** (~4 refs): Has `BackfillTarget::VaultNotes` enum variant. Rename to `BackfillTarget::Memories`. Update match arms, SQL, display.

9. **context/injection.rs** (~8 refs): VaultNote enum + SQL string + function. Update all.

10. **cache/sqlite.rs** (~8 refs): SQL schema for local cache has `vault_notes` table. Update SQL CREATE TABLE and all references. NOTE: The local SQLite cache table name should be updated to `memories` for consistency (the cache mirrors the PostgreSQL schema).

11. **cache/hybrid.rs** (~20 refs): Module import + function calls + struct refs + SQL. This is the largest consumer — read carefully and update every reference.

12. **cache/sync_queue.rs** (~2 refs): String literal in test. Update.

13. **db/tests.rs** (~10 refs): Module import + function calls. Update all vault_notes references.

14. **db/documents.rs** (~2 refs): Comments only. Update for accuracy.

After all files are updated, run: `cd /root/dpn-core && cargo check 2>&1`

If cargo check fails, read the error messages and fix any remaining references. Common issue: a `VaultNote` or `vault_notes` reference was missed in a match arm or type annotation.

After cargo check passes, run: `cd /root/dpn-core && cargo test -- --test-threads=1 2>&1`

Tests may fail if they reference the live database (which now has `memories` table, not `vault_notes`). That's expected and correct — the tests should now work because the SQL has been updated to `FROM memories`.
  </action>
  <verify>
    <automated>cd /root/dpn-core && cargo check 2>&1 | tail -5 && grep -r "vault_notes" src/ --include="*.rs" -l | grep -v "target/" || echo "NO_REMAINING_REFS"</automated>
  </verify>
  <acceptance_criteria>
    - `cd /root/dpn-core && cargo check` succeeds with no errors
    - `grep -r "FROM vault_notes" src/ --include="*.rs"` returns no results (zero SQL references to old table)
    - `grep -r "VaultNote" src/ --include="*.rs" -l` returns no results (zero old struct references)
    - `grep -r "pub mod vault_notes" src/ --include="*.rs"` returns no results
    - Comments may still mention "vault_notes" in historical context — that's acceptable. But no code references.
    - `cargo test -- --test-threads=1` passes (or fails only on unrelated tests)
  </acceptance_criteria>
  <done>All 15 dpn-core consumer files updated, cargo check passes, no remaining vault_notes references in production code</done>
</task>

</tasks>

<verification>
- `cd /root/dpn-core && cargo check` succeeds
- `cd /root/dpn-core && cargo test -- --test-threads=1` passes
- `grep -r "FROM vault_notes" /root/dpn-core/src/ --include="*.rs"` returns nothing
- `grep -r "VaultNote" /root/dpn-core/src/ --include="*.rs"` returns nothing (excluding comments)
- `/root/dpn-core/src/db/memories.rs` exists with Memory and MemoryLight structs
</verification>

<success_criteria>
dpn-core compiles cleanly with all vault_notes references replaced by memories. The Memory and MemoryLight structs include compression_tier and compressed_from fields. All 15 consumer files reference the new module path and type names.
</success_criteria>

<output>
After completion, create `.planning/phases/18-memories-rename/18-02-SUMMARY.md`
</output>
