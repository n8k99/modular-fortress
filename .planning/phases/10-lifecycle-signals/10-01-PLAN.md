---
phase: 10-lifecycle-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /opt/dpn-api/src/handlers/af64_agents.rs
  - /opt/project-noosphere-ghosts/lisp/runtime/energy.lisp
autonomous: true
requirements:
  - LIFE-01
  - LIFE-03
user_setup: []

must_haves:
  truths:
    - "list_agents API response includes metadata JSONB for each agent"
    - "PATCH /api/agents/:id/state with metadata merges into existing metadata (does not overwrite)"
    - "energy.lisp has an :idle-transition reward of +12"
  artifacts:
    - path: "/opt/dpn-api/src/handlers/af64_agents.rs"
      provides: "metadata in list_agents response + metadata PATCH merge support"
      contains: "metadata"
    - path: "/opt/project-noosphere-ghosts/lisp/runtime/energy.lisp"
      provides: ":idle-transition energy reward"
      contains: "idle-transition"
  key_links:
    - from: "/opt/dpn-api/src/handlers/af64_agents.rs"
      to: "agent_state.metadata"
      via: "SQL query includes metadata column + PATCH merges with ||"
      pattern: "COALESCE.*metadata.*\\|\\|"
---

<objective>
Add metadata to the list_agents API response and metadata PATCH merge support, plus the idle-transition energy reward constant.

Purpose: The tick engine needs to read lifecycle_state from agent metadata via the list endpoint, and write it back via PATCH with merge semantics. The energy reward is needed for the one-time idle transition boost.
Output: Rust handler changes + Lisp energy reward constant
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-lifecycle-signals/10-CONTEXT.md
@.planning/phases/10-lifecycle-signals/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metadata to list_agents response and metadata PATCH merge</name>
  <read_first>
    /opt/dpn-api/src/handlers/af64_agents.rs
  </read_first>
  <files>/opt/dpn-api/src/handlers/af64_agents.rs</files>
  <action>
Two changes to af64_agents.rs:

1. **list_agents response** (around line 27-46): The SQL query already selects `s.metadata` (line 18), but the JSON serialization omits it. Add `"metadata"` to the `serde_json::json!` block in the `rows.iter().map` closure:
   ```rust
   "metadata": r.get::<Option<Value>, _>("metadata"),
   ```
   Add this after the `"ticks_alive"` line (line 44), before the closing `})`.

2. **update_state handler** (around line 127-172): The `StateUpdate` struct already has `metadata: Option<Value>` (line 122), but the handler does not process it. Add a metadata merge block after the `ticks_alive` block (after line 166):
   ```rust
   if let Some(ref metadata) = body.metadata {
       sqlx::query(
           "UPDATE agent_state SET metadata = COALESCE(metadata, '{}'::jsonb) || $1::jsonb WHERE agent_id = $2"
       )
       .bind(metadata)
       .bind(&id)
       .execute(&pool)
       .await
       .map_err(|e| ApiError::Database(e.to_string()))?;
   }
   ```

   IMPORTANT: Use `COALESCE(metadata, '{}'::jsonb) || $1::jsonb` for merge semantics, NOT `SET metadata = $1` which would overwrite. Per research Pitfall 1.

Per D-01, D-02: These API changes enable the tick engine to read/write lifecycle_state in agent metadata.
  </action>
  <verify>
    <automated>cd /opt/dpn-api && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` succeeds with no errors
    - `grep -c '"metadata"' /opt/dpn-api/src/handlers/af64_agents.rs` returns at least 2 (one in list_agents, one in get_agent)
    - `grep -c 'COALESCE(metadata' /opt/dpn-api/src/handlers/af64_agents.rs` returns 1
    - `grep 'body.metadata' /opt/dpn-api/src/handlers/af64_agents.rs` finds the metadata PATCH block
  </acceptance_criteria>
  <done>list_agents returns metadata field, PATCH merges metadata with COALESCE, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Add :idle-transition energy reward</name>
  <read_first>
    /opt/project-noosphere-ghosts/lisp/runtime/energy.lisp
  </read_first>
  <files>/opt/project-noosphere-ghosts/lisp/runtime/energy.lisp</files>
  <action>
Add the `:idle-transition` reward to the `+energy-rewards+` hash table in energy.lisp.

After the existing line `(setf (gethash :peer-ack table) 4)` (line 27), add:
```lisp
(setf (gethash :idle-transition table) 12)
```

The value +12 is within the D-06 range of 10-15 (Claude's discretion per CONTEXT.md). This reward is applied once when an agent transitions from active to idle (per D-07: transition-only, not every idle tick).

Per D-06, D-07, LIFE-03: Energy boost on idle transition ensures idle agents quickly reach working/prime tier for immediate delegation.
  </action>
  <verify>
    <automated>grep "idle-transition" /opt/project-noosphere-ghosts/lisp/runtime/energy.lisp</automated>
  </verify>
  <acceptance_criteria>
    - `grep ':idle-transition' /opt/project-noosphere-ghosts/lisp/runtime/energy.lisp` returns the line `(setf (gethash :idle-transition table) 12)`
    - The value is 12 (within D-06 range of 10-15)
  </acceptance_criteria>
  <done>:idle-transition reward exists in +energy-rewards+ hash table with value 12</done>
</task>

</tasks>

<verification>
- `cd /opt/dpn-api && cargo check` passes
- list_agents response includes metadata field
- PATCH handler supports metadata merge
- energy.lisp has :idle-transition reward of 12
</verification>

<success_criteria>
- Rust API compiles cleanly with metadata in list_agents and PATCH merge support
- Energy reward constant ready for use by tick engine in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/10-lifecycle-signals/10-01-SUMMARY.md`
</output>
