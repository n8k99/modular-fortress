---
phase: 26-runtime-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /opt/project-noosphere-ghosts/lisp/runtime/action-executor.lisp
autonomous: true
gap_closure: true
requirements: [STAB-01]

must_haves:
  truths:
    - "execute-work-task variables agent-id, content, task, stage, tools-executed are in scope through line 612"
    - "SBCL loads action-executor.lisp with zero WARNINGs about undefined variables"
    - "Live tick cycle produces zero action-error entries mentioning AGENT-ID is unbound"
  artifacts:
    - path: "/opt/project-noosphere-ghosts/lisp/runtime/action-executor.lisp"
      provides: "Fixed outer let* scope in execute-work-task"
      contains: "(error () nil)))"
  key_links:
    - from: "/opt/project-noosphere-ghosts/lisp/runtime/action-executor.lisp"
      to: "tick-engine.lisp"
      via: "execute-cognition-result dispatches to execute-work-task"
      pattern: "execute-work-task"
---

<objective>
Fix the outer let* scope bug in execute-work-task that was introduced by the STAB-01 paren fix in Plan 01.

Purpose: The Plan 01 paren fix (commit 06b72c7) correctly placed the handler-case error clause but over-closed the outer let* binding, leaving agent-id/content/task/stage/tools-executed unbound for lines 498-612. Every work_task execution hits "AGENT-ID is unbound" at runtime.

Output: A corrected action-executor.lisp where the outer let* stays open through the json-object return on line 612, verified by SBCL loading with zero undefined-variable WARNINGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-runtime-stability/26-VERIFICATION.md
@.planning/phases/26-runtime-stability/26-01-SUMMARY.md
@.planning/phases/26-runtime-stability/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix outer let* scope in execute-work-task</name>
  <files>/opt/project-noosphere-ghosts/lisp/runtime/action-executor.lisp</files>
  <read_first>
    - /opt/project-noosphere-ghosts/lisp/runtime/action-executor.lisp (lines 470-613)
    - /root/.planning/phases/26-runtime-stability/26-VERIFICATION.md
  </read_first>
  <action>
Fix the paren scope bug on line 497 of action-executor.lisp in the execute-work-task function.

**Root cause:** Line 497 currently reads `(error () nil))))` with 4 closing parens. The 4th paren closes the outer `let*` from line 472 prematurely, making agent-id, content, task, stage, and tools-executed unbound for all code on lines 498-612.

**Concrete fix — two edits:**

1. Line 497: Change `(error () nil))))` to `(error () nil)))` (remove the last closing paren — 3 parens instead of 4). This keeps the outer `let*` open. After this edit, line 497 closes: `(error () nil)` + `handler-case` + `(when tool-results` = 3 closing parens. The `(let ((tool-results ...` from line 483 was already closed by the 5th paren on line 496.

2. Line 612: Change `:response (subseq content 0 (min 200 (length content)))))` to `:response (subseq content 0 (min 200 (length content))))))` (add one closing paren — 6 parens instead of 5). This closes the outer `let*` at the end of the function where it belongs.

**Paren depth verification after fix:**
- Line 472 (let*): depth 2 -> 3 (let* body at depth 2)
- Line 476 (tools-executed 0)): depth -> 2 (inside let* body)
- Line 497 (error () nil))): depth -> 2 (stays inside let* body, NOT 1)
- Lines 498-611: all at depth >= 2 (inside let* body, agent-id/content in scope)
- Line 612: depth -> 0 (closes json-object + let* + defun)

After editing, commit in /opt/project-noosphere-ghosts with message:
`fix(action-executor): close outer let* at end of execute-work-task, not line 497`

Do NOT modify any other lines or add any other changes. This is a surgical 2-character edit (remove one paren from line 497, add one paren to line 612).
  </action>
  <verify>
    <automated>cd /opt/project-noosphere-ghosts/lisp && sbcl --noinform --non-interactive --eval '(require :asdf)' --eval '(dolist (f (list "/opt/innatescript/src/packages" "/opt/innatescript/src/types" "/opt/innatescript/src/conditions" "/opt/innatescript/src/parser/tokenizer" "/opt/innatescript/src/parser/parser" "/opt/innatescript/src/eval/resolver" "/opt/innatescript/src/eval/evaluator")) (load (format nil "~a.lisp" f)))' --eval '(load "packages.lisp")' --eval '(dolist (f (list "util/json" "util/pg" "util/http" "runtime/db-client" "runtime/cognition-types" "runtime/db-auxiliary" "runtime/db-conversations" "runtime/db-tasks" "runtime/task-scheduler" "runtime/perception" "runtime/action-planner" "runtime/cognition-broker" "runtime/noosphere-resolver" "runtime/innate-builder" "runtime/action-executor")) (load (format nil "~a.lisp" f)))' --eval '(format t "LOAD OK~%")' 2>&1 | grep -E "WARNING|LOAD OK|undefined variable"</automated>
  </verify>
  <acceptance_criteria>
    - SBCL load output contains "LOAD OK" and zero lines containing "WARNING" or "undefined variable"
    - Line 497 of action-executor.lisp reads exactly `          (error () nil)))` (3 closing parens after nil)
    - Line 612 ends with 6 closing parens: `:response (subseq content 0 (min 200 (length content))))))`
    - git log --oneline -1 in /opt/project-noosphere-ghosts shows the fix commit
  </acceptance_criteria>
  <done>execute-work-task outer let* scope extends through line 612; SBCL loads with zero undefined-variable warnings; commit pushed to em-droplet branch</done>
</task>

<task type="auto">
  <name>Task 2: Verify live tick cycle with no AGENT-ID unbound errors</name>
  <files></files>
  <read_first>
    - /root/.planning/phases/26-runtime-stability/26-VERIFICATION.md (human_verification section)
  </read_first>
  <action>
Restart the noosphere-ghosts PM2 process and verify a clean tick cycle:

1. Run: `pm2 restart noosphere-ghosts`
2. Wait 120 seconds for at least one full tick cycle to complete
3. Check logs: `pm2 logs noosphere-ghosts --lines 200 --nostream`
4. Verify:
   - Zero occurrences of "AGENT-ID is unbound" in logs
   - Zero occurrences of "CONTENT is unbound" in logs
   - Zero occurrences of "undefined variable" in compile output
   - At least one "[work-output]" or "[work-task-mutations]" line appears (proves execute-work-task ran successfully with variables in scope)
5. Stop ghosts after verification: `pm2 stop noosphere-ghosts`

Per D-08, verification is against the live system. Ghosts will consume Claude API budget while running.
  </action>
  <verify>
    <automated>pm2 logs noosphere-ghosts --lines 200 --nostream 2>&1 | grep -c "AGENT-ID is unbound"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "AGENT-ID is unbound" returns count 0
    - grep for "CONTENT is unbound" returns count 0
    - At least one tick cycle completed (log shows "[tick-report]" or "[tick-end]" or equivalent tick completion marker)
    - noosphere-ghosts process stopped after verification
  </acceptance_criteria>
  <done>Live tick cycle completes with execute-work-task running successfully; zero unbound-variable errors; STAB-01 fully resolved</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. action-executor.lisp has correct paren scope — outer let* closes at line 612
2. SBCL loads with zero WARNINGs about undefined variables
3. Live tick proves execute-work-task runs with agent-id/content/task/stage/tools-executed in scope
4. The VERIFICATION.md gap (Truth 1: "execute-work-task returns json-object from correct let* scope") is resolved
</verification>

<success_criteria>
- SBCL loads action-executor.lisp with zero undefined-variable warnings
- Live tick cycle produces zero "AGENT-ID is unbound" action-error entries
- At least one work_task action completes successfully (visible in PM2 logs)
- Fix committed to em-droplet branch in /opt/project-noosphere-ghosts
</success_criteria>

<output>
After completion, create `.planning/phases/26-runtime-stability/26-03-SUMMARY.md`
</output>
