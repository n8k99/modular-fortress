---
phase: 26-runtime-stability
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: []
autonomous: false
requirements: [STAB-01, STAB-02]

must_haves:
  truths:
    - "A full tick cycle completes without runtime errors on the live system"
    - "No PQescapeLiteral failures in tick logs (UTF-8 fix working)"
    - "No description column errors in tick logs (column removed from queries)"
    - "No type errors from parse-iso8601 or task-ready-p (NULL guards working)"
  artifacts: []
  key_links:
    - from: "PM2 noosphere-ghosts process"
      to: "master_chronicle PostgreSQL"
      via: "libpq FFI direct connection"
      pattern: "tick.*complete"
---

<objective>
Restart the noosphere-ghosts PM2 process and verify a complete tick cycle runs without runtime errors on the live system.

Purpose: Per D-07 and D-08, verification must be against the live noosphere-ghosts process, not a test harness. This proves all 9 fixes work together under real conditions with real agents perceiving real data from master_chronicle.

Output: Verified clean tick cycle, phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-runtime-stability/26-01-SUMMARY.md
@.planning/phases/26-runtime-stability/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart noosphere-ghosts and capture tick cycle output</name>
  <files>None (live system verification)</files>
  <read_first>
    .planning/phases/26-runtime-stability/26-RESEARCH.md (Verification Approach section)
    .planning/phases/26-runtime-stability/26-VALIDATION.md (Full suite command)
  </read_first>
  <action>
    Restart the PM2 process and wait for at least one complete tick cycle:

    ```bash
    pm2 restart noosphere-ghosts
    ```

    Wait approximately 60-90 seconds for a tick to complete. Then capture logs:

    ```bash
    pm2 logs noosphere-ghosts --lines 200 --nostream
    ```

    Scan output for these error indicators (per Research doc):
    - `PQescapeLiteral failed` -- would indicate UTF-8 fix not working
    - `description` column errors -- would indicate db-tasks fix not working
    - `[tick-error]` or `[action-error]` -- general tick failures
    - Type errors mentioning `parse-iso8601` or `NIL is not a string`
    - Any unhandled condition or debugger entry

    If errors found: diagnose the specific fix that failed, read the relevant committed file, and fix. Then restart PM2 and re-verify.

    If clean: the phase is complete. Stop the ghosts if Nathan wants them stopped (they consume Claude API budget):
    ```bash
    pm2 stop noosphere-ghosts
    ```

    NOTE per Pitfall 3: Starting ghosts runs live ticks against production DB with real agents consuming Claude API budget (~$0.50/request). This is expected and desired per D-08.
  </action>
  <verify>
    <automated>pm2 logs noosphere-ghosts --lines 100 --nostream 2>&1 | grep -ciE "tick-error|action-error|PQescapeLiteral failed|debugger|unhandled"</automated>
  </verify>
  <acceptance_criteria>
    - `pm2 logs noosphere-ghosts --lines 100 --nostream 2>&1 | grep -ciE "tick-error|action-error|PQescapeLiteral failed|debugger|unhandled"` returns 0 (zero matches)
    - PM2 logs contain at least one complete tick cycle (look for "[tick" markers or timestamp progression)
    - No SBCL debugger entry in logs (would show as "debugger invoked" or "restarts (invocations)")
  </acceptance_criteria>
  <done>At least one tick cycle completed without runtime errors. All 9 fixes verified working together on the live system.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of live tick cycle</name>
  <files>None</files>
  <action>
    Present verification results to user. The executor has already restarted noosphere-ghosts and captured tick logs. User confirms the tick output looks correct and the phase is complete.

    What was built: Fixed the execute-work-task paren scope bug (STAB-01) and committed all 9 tick engine fixes (STAB-02). Restarted noosphere-ghosts and verified a clean tick cycle on the live system.

    How to verify:
    1. Check PM2 status: `pm2 list` -- noosphere-ghosts should show as running (or stopped if executor stopped it after verification)
    2. Check recent logs: `pm2 logs noosphere-ghosts --lines 50 --nostream` -- should show tick activity without errors
    3. Check commits: `cd /opt/project-noosphere-ghosts && git log --oneline -5` -- should show 4 fix() commits
    4. Optionally: leave ghosts running if you want live operation, or `pm2 stop noosphere-ghosts` to save API budget
  </action>
  <verify>User types "approved" or describes issues</verify>
  <acceptance_criteria>
    - User confirms tick output is clean
    - User approves phase completion
  </acceptance_criteria>
  <done>Phase 26 approved by user. Runtime stability verified on live system.</done>
</task>

</tasks>

<verification>
1. PM2 logs show at least one complete tick without error markers
2. Zero matches for: tick-error, action-error, PQescapeLiteral failed, debugger, unhandled
3. Human confirms tick output looks correct
</verification>

<success_criteria>
- STAB-01: execute-work-task runs in a live tick without the paren bug causing issues (handler-case catches errors)
- STAB-02: All fixes work together in production -- no regressions from any of the 9 file changes
- D-07 satisfied: Complete tick cycle on live system without runtime errors
- D-08 satisfied: Verified against live PM2 process, not a test harness
</success_criteria>

<output>
After completion, create `.planning/phases/26-runtime-stability/26-02-SUMMARY.md`
</output>
